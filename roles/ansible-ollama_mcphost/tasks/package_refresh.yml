---
- name: Auto-refresh package metadata (import keys)
  ansible.builtin.command:
    argv: "{{ package_refresh_command }}"
  become: true
  changed_when: false
  when:
    - package_auto_refresh
    - ansible_facts is defined
    - ansible_facts.get('pkg_mgr', '') == 'zypper'
